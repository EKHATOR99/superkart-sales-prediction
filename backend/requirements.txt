# flask==2.2.2 / Werkzeug==2.2.2 are pinned together deliberately - newer Werkzeug versions
# removed `url_quote`, which breaks `import flask` on this exact flask version. Before a
# production rollout, budget time to upgrade to a current flask/Werkzeug pair (and re-test),
# since older pinned versions can carry unpatched CVEs.
pandas==2.2.2
numpy==2.0.2
scikit-learn==1.6.1
xgboost==2.1.4
joblib==1.4.2
Werkzeug==2.2.2
flask==2.2.2
gunicorn==22.0.0
requests==2.32.4
